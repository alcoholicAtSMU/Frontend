import React, { useEffect, useState } from "react";
import axios from "axios";
import { useNavigate } from "react-router-dom";
import "./createBar.css";

const CreateBar = () => {
  const navigate = useNavigate();
  return <div>CreateBar</div>;
};

export default CreateBar;
